class Node {
  constructor(data, next = null){
    this.data = data;
    this.next = next;
  };
};

class linkedList {
  constructor() {
    this.head = null;
    this.size = 0;
  };

  // Insert first node

  // Insert last node

  // Insert at index

  // Get at index

  // Remove at index

  // Clear list

  // Print list data

};